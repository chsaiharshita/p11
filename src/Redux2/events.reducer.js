/*const initialState = {
  isLoading: false,
  events: [],
  errorMessage: null,
};
*/
/*export const eventsReducer = (state = initialState, action) => {
  switch (action.type) {
    case "EVENTS_REQUEST":
      return { ...state, isLoading: true };
    case "EVENTS_SUCCESS":
      return { isLoading: false, events: action.payload, errorMessage: null };
    case "EVENTS_FAIL":
      return { isLoading: false, events: [], errorMessage: action.payload };
    default:
      return state;
  }
};*/
